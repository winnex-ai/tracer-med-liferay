# madhava-service — ponte Madhava -> Liferay
# winnex-madhava >=1.9.1 (witness certificate) depende APENAS de numpy (verificado no pyproject).
# O motor NÃO faz embedding de texto; recebe vetores float32 prontos.
winnex-madhava>=1.9.1
fastapi>=0.100
uvicorn>=0.20
numpy>=1.24
